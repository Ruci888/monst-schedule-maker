# v1.1.0-beta.9.6 更新内容

動画OCRの処理順を見直し、同じ降臨カードへ何度もTesseractを実行しないようにしました。

## 主な変更

- フレーム内のカード画像を先に検出し、画像ハッシュで重複を除外
- 同一カードは最も鮮明な画像だけをOCR対象として保持
- 「高速抽出」と「精密抽出」の2モードを管理画面へ追加
- 高速抽出は各カードを原則1回だけOCR
- 精密抽出は高速OCRで不足した日時・名前・難易度だけを追加OCR
- 進捗バーと処理状況の表示を追加
- 120秒を超えた動画処理を安全に停止
- 動画の確認間隔を1秒へ変更し、最大確認フレームを120枚へ制限

## 推奨する確認手順

1. 最初は「高速抽出」を選択する
2. 以前と同じ動画をアップロードする
3. 進捗表示、完了までの時間、確認候補数を確認する
4. 日時・名前・属性・難易度の認識結果を確認する
5. 不足が多い場合だけ「精密抽出」を試す

高速抽出でも速度または精度が不足する場合は、次版で複数スクリーンショットの一括読取方式へ切り替えます。

## ZIP内の5ファイル

### 更新に必要

1. `app.py`
2. `admin_app.py`
3. `video_schedule_extractor.py`

### 動作確認・説明用

4. `test_video_schedule_extractor.py`
5. `UPDATE_v1.1.0-beta.9.6.md`

GitHubには5ファイルすべてを上書き・追加して構いません。

## GitHubのCommit message

`Speed up staged video OCR for beta 9.6`
