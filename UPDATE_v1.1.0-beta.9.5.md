# v1.1.0-beta.9.5 更新内容

## GitHubで上書きするファイル

- `app.py`
- `admin_app.py`
- `video_schedule_extractor.py`

上記3ファイルが公開アプリの更新に必要です。

ZIPには、動作確認用として次の2ファイルも含まれます。

- `test_video_schedule_extractor.py`
- `UPDATE_v1.1.0-beta.9.5.md`

したがって、ZIP内は合計5ファイルです。

## 主な変更

- OCR結果を`JSON`へ即時保存しない方式へ変更
- OCRに失敗したカードも、切り出し画像があれば一時確認候補として保持
- 管理画面で候補ごとの切り出し画像を表示
- 画像を見ながら、日付・キャラ名・属性・難易度などを編集可能
- 保存対象の候補だけを選択して承認待ちへ保存
- 一時候補を全部破棄しても、既存JSONは変更しない
- 切り出し画像は`JSON`へ保存せず、画面の一時確認中だけ保持
- OCR日付が`3/20`などに化けても、カード画像の特徴で動画内重複を統合
- 管理画面の結果表示を「自動補正」「画像確認」「確認候補」中心へ変更
- 公開画面のバージョン表記を`v1.1.0-beta.9.5`へ更新

## 更新後の確認手順

1. GitHubで上記3ファイルを上書きする
2. Commit messageに`Improve video review workflow for beta 9.5`と入力する
3. 管理画面が再起動したら、録画内の最初の日程を指定する
4. 同じ画面録画をアップロードして抽出する
5. 「動画から切り出した一時確認候補」が表示されることを確認する
6. 候補を切り替え、カード画像が1枚ずつ表示されることを確認する
7. 表で日付・名前・属性・難易度を修正する
8. 保存する候補の「保存対象」をチェックする
9. 「選択した修正済み候補を承認待ちへ保存」を押す
10. 下部の「降臨候補」一覧に保存した候補だけが追加されたことを確認する

## 注意

- 抽出直後の一時候補は、ページ再起動やアプリのスリープで消える場合があります。
- 切り出し画像は永続保存しません。
- OCR結果が不完全でも、画像を見て管理者が修正できることを優先した更新です。
- `requirements.txt`の変更はありません。

## 確認済み

- Python構文チェック：合格
- OCR後処理・重複排除・日付補正などの自動テスト19件：合格
- 実動画テスト：作業環境にOpenCV／pytesseractがないため未実施
