# v1.1.0-beta.9.16 更新内容

## 今回の修正

- 降臨マスターのまとめて登録では、上部で選択した降臨カテゴリを全行へ適用するように変更しました。
- 各行の入力形式を `名前｜属性｜難易度` または `名前｜属性｜難易度｜読み` に統一しました。
- 各行へ降臨カテゴリを手入力する必要はありません。旧5項目形式は誤登録せず、その行を理由付きエラーとして表示します。
- `通常降臨` を選び、難易度に `超絶` を指定した登録を正しい組み合わせとして扱います。
- 10件以上をまとめて登録した場合も、各キャラについて次の判定を一覧表示します。
  - `新規追加`
  - `既存更新`
  - `入力重複`
  - `エラー`
- 一部の行に入力エラーがあっても、正常な行は登録対象として保存します。
- 同じ一括入力内で名前・難易度が重複した行は、2件目以降を保存せず `入力重複` として表示します。
- 既存の重複マスター削除機能はそのまま利用できます。

## 入力例

上部の降臨カテゴリで `通常降臨` を選択してから、次のように入力します。

```text
スノーマン｜木｜究極
仙丹｜木｜超絶｜せ
ストラテジー｜水｜爆絶
```

4項目目は任意の読みです。漢字で始まる名前は、五十音分類に使う先頭1文字だけでも登録できます。

## GitHubで上書きするファイル

必須の更新ファイルは次の3ファイルです。

- `app.py`
- `admin_app.py`
- `quest_master.py`

テストをリポジトリでも保持する場合は、次のファイルも上書きしてください。

- `test_quest_master.py`

`UPDATE_v1.1.0-beta.9.16.md` は説明書のため、GitHubへの配置は任意です。

## 確認結果

- Python構文検査：成功
- 単体テスト：90件すべて成功

## GitHub Commit message

```text
fix: improve bulk quest master registration (v1.1.0-beta.9.16)
```
