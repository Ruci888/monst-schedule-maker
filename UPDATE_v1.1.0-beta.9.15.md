# v1.1.0-beta.9.15 更新内容

## 更新が必要なファイル

GitHub上で次の3ファイルを必ず上書きしてください。

- `app.py`
- `admin_app.py`
- `requirements.txt`

`requirements.txt` は、タブをラベル指定で維持するために必要な
Streamlit 1.50以上を指定しています。更新後はStreamlit Cloudの再起動・再デプロイを
待ってから管理画面を確認してください。

`UPDATE_v1.1.0-beta.9.15.md` は説明書のため、GitHubへの配置は任意です。
前版と同一のその他コードファイルは更新不要です。

## 主な変更

### 1. スマホで抽出タブからイベント管理タブへ移動する問題

- タブ順を動的に入れ替える方式を廃止
- タブ順を常に固定
- 画像選択・抽出・候補確認中は「自動取得候補・失敗ログ」を
  ラベル指定で既定選択
- 「次へ」などによる再実行後も抽出作業を継続可能

### 2. 抽出候補一覧と承認待ち保存

- 画像マスター登録とは別に「抽出候補一覧・承認待ち保存」を追加
- 各候補を、日付・名前・属性・難易度・降臨カテゴリの1行で表示
- 一覧のチェックボックスで承認待ちへ保存する候補を選択
- 一覧直下から、選択した候補だけを承認待ちへ保存
- 「全候補を保存対象にする」も一覧内へ移動

### 3. 画像内の最初の日程

- スクリーンショット抽出画面から手動入力項目を削除
- 現在日の7日前を内部基準日として、現在日前後約1週間の日付を自動判定
- 大きく外れたOCR日付を未確定へ回す安全確認は維持
- 動画抽出側の日付指定は変更なし

### 4. スマホ向け件数表示

- 6個の大きなメトリクス表示を廃止
- 候補・自動補正・画像確認・重複・除外件数を1つのコンパクトな行へ統合
- スマホ幅では必要に応じて自然に折り返して表示

## 確認結果

- Python構文検査：成功
- 単体テスト：88件すべて成功
- `st.tabs(default=...)` 対応バージョン：Streamlit 1.50以上

## GitHub Commit message

```text
fix: improve mobile extraction review flow (v1.1.0-beta.9.15)
```
