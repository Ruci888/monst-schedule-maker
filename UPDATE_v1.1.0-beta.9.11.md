# v1.1.0-beta.9.11 更新内容

前回までに確定した改善事項を、降臨マスター中心の一続きの運用として実装しました。

## 実装した内容

### 1. 永続的な降臨マスター

- `quest_master.json` に登録済み降臨を保存
- 名前＋難易度の組み合わせで自動登録・更新
- 同名別難易度は内部 `quest_id` で区別
- 名前、属性、難易度、降臨カテゴリ、掲載グループ、情報源、公開状態、登録日時、更新日時を保持
- 公開済みの既存降臨から初期マスターを作成・補完可能

### 2. 候補承認とマスター登録を連動

- 保存対象の候補を承認・公開すると、同時にマスターへ自動登録
- マスター登録用チェックは不要
- 登録済みの名前＋難易度なら、同じ `quest_id` の内容を更新
- 管理画面から直接編集・保存した降臨もマスターへ同期

### 3. スクリーンショット認識でマスターを優先

- `quest_master.json` の確定値を、既存日程や承認待ち候補より優先して照合
- 名前が多少誤認識されても、近いマスターから名前・属性・難易度・カテゴリを補正
- 同名別難易度はOCRで取得した難易度も使って `quest_id` を選択
- マスターで確定できない同名候補は勝手に選ばず確認へ回す

### 4. 登録済み降臨を検索して再利用

- 名前・難易度・カテゴリ・掲載グループなどから検索
- 掲載日と開始時間だけを指定して日程へ追加
- 終了時間は必ず翌日11:59に自動設定
- 名前・属性・難易度・カテゴリ・`quest_id` はマスターから自動入力

### 5. 通常とコラボ・期間限定の寿命を分離

- 通常降臨・高難易度マスターは期間終了日を持たず継続保存
- コラボ・イベント期間限定だけ開催方式と期間終了日を保持
- 期限切れマスターは通常検索から除外し、「期限切れも表示」で確認可能
- 期限後も自動削除せず、管理者が確認して削除

### 6. 確認付き削除

- マスター削除前に「本当に削除しますか？」を表示
- 回答は「はい／いいえ」から選択
- 承認待ち候補の削除にも同じ確認を追加
- 独立したゴミ箱は追加せず、既存のローカルバックアップ／GitHub履歴を利用

### 7. 候補確認画面を6項目へ整理

表示する項目は次の6つだけです。

1. 保存対象
2. 日付
3. 名前
4. 属性
5. 難易度
6. 降臨カテゴリ

開催方式、期間終了日、情報源、OCR原文、確認理由などは画面に並べず、内部データとして保持します。名前を含む5つの内容項目は編集可能です。

### 8. 録画取得画面を撤去

- 管理画面から画面録画のアップロード・抽出導線を削除
- スクリーンショット取得とマスター照合へ運用を集約

## 最初の導入手順

1. ZIP内のコードファイルをGitHubの同名ファイルへ上書きする。
2. `quest_master.py` と `quest_master.json` は新規追加する。
3. 管理画面を開き、「降臨マスター」タブを選ぶ。
4. 「公開済み降臨からマスターを初期登録・補完」を一度押す。
5. 以後は候補承認時にマスターが自動更新される。

`quest_master.json` は初回だけ空ファイルを追加します。今後の更新で既にデータが入っている場合は、空ファイルで上書きしないでください。

## 更新ファイル

### 実行に必要なファイル

1. `app.py`
2. `admin_app.py`
3. `data_manager.py`
4. `github_storage.py`
5. `quest_master.py`
6. `quest_master.json`
7. `schedule_utils.py`
8. `video_schedule_extractor.py`

### テスト・説明ファイル

9. `test_quest_master.py`
10. `test_schedule_utils.py`
11. `test_video_schedule_extractor.py`
12. `UPDATE_v1.1.0-beta.9.11.md`

## 確認結果

- Python構文検査：成功
- 単体テスト：70件すべて成功
- 管理画面から録画取得UIがなくなっていることを静的確認
- 候補画面の6項目、マスター自動登録、検索再利用、期限判定、確認付き削除を静的確認

## GitHubコミットメッセージ例

`feat: add persistent quest master workflow (v1.1.0-beta.9.11)`
