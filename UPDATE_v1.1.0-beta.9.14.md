# v1.1.0-beta.9.14 更新内容

## 更新が必要なファイル

GitHub上で次の4ファイルを上書きしてください。

- `app.py`
- `admin_app.py`
- `quest_master.py`
- `test_quest_master.py`

`UPDATE_v1.1.0-beta.9.14.md` は説明書のため、GitHubへの配置は任意です。
前版ZIPに含まれていた `video_schedule_extractor.py` と
`test_video_schedule_extractor.py` は今回変更していないため、上書き不要です。

## 主な変更

- スクリーンショット選択・抽出・候補の「次へ」などで再実行されても、
  抽出作業中は「自動取得候補・失敗ログ」タブを維持
- マスター選択欄の案内文を実データから外し、選択後すぐ文字入力して検索可能に変更
- マスターへ任意の「読み」を保存可能に変更
- カタカナ名は名前から自動分類し、漢字名は登録した読みの先頭で
  あ行・か行・さ行などへ分類
- 降臨マスター画面と候補へのマスター割り当て画面に五十音絞り込みを追加
- 読みでもマスターを検索可能に変更
- まとめて登録を `名前｜属性｜難易度｜カテゴリ｜読み` の5項目に対応
- 「全候補を保存対象にする」で発生していたStreamlitの状態更新エラーを修正
- 候補IDを一度生成した後は固定し、保存対象の切り替えでウィジェットIDが変わらないよう修正
- カードが欠けている・表示率98%未満・不鮮明な場合に画像マスターへ保存しない既存判定を維持

## 読みの登録例

1体ずつ登録する場合は、「読み（任意）」へ `せ` または `せんたん` と入力します。

まとめて登録する場合：

```text
仙丹｜木｜超絶｜通常降臨｜せ
浄化の仏神 金剛夜叉明王廻｜光｜超絶・廻｜高難易度・注目｜じ
```

読みを登録していない漢字始まりのマスターは「未分類」に表示され、
詳細設定から後で読みを追加できます。

## 確認結果

- Python構文検査：成功
- 単体テスト：88件すべて成功

## GitHub Commit message

```text
fix: keep extraction review active and add kana master search (v1.1.0-beta.9.14)
```
