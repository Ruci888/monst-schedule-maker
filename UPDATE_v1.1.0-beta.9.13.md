# v1.1.0-beta.9.13 更新内容

マスターを先に準備し、完全に写った切り出しカードを1回割り当てることで、次回以降はOCR名に依存せず画像特徴から名前・属性・難易度・カテゴリを補正できるようにしました。

## 実装内容

### 1. カード画像特徴をマスターへ割り当て

- スクリーンショット抽出後、候補の切り出しカードを1件ずつ表示
- 登録済み降臨マスターを検索してカードへ割り当て
- 完全なカードの場合は、選択したマスターへ画像特徴を同時登録
- 肖像、名前領域、カード全体の特徴と `quest_id` の対応を保存
- 元のスクリーンショットやJPEG画像そのものはJSONへ保存せず、照合用の小さな特徴値だけを保存
- 1マスターにつき直近8件までの画像特徴を保持

### 2. OCR名が壊れていても画像から自動補正

- 次回の抽出ではキャラ肖像の特徴を先にマスターと照合
- 一致した `quest_id` から名前・属性・難易度・カテゴリを取得
- 「スノーマン」が全く別のOCR文字列になっても画像一致で補正可能
- 近いマスターが複数ある場合は自動確定せず、手動選択へ回す
- 画像一致しない場合だけ従来の名前類似照合を補助的に使用

### 3. 欠けたカードを画像マスターへ保存しない

次の条件を満たしたカードだけ画像特徴を登録します。

- 上端と下端が入っている
- 表示範囲が98%以上
- 下部メニューなどが重なっていない
- 肖像領域を識別できる
- 最低限の鮮明度を満たしている

不完全なカードは既存マスターとの照合には使用できますが、新しい学習データとしては保存しません。

### 4. 誤った画像間重複統合を停止

- OCR前の画像間類似だけでは候補を削除しない
- OCR名が同じだけでも未確定候補を統合しない
- マスター一致後、同じ日付・同じ `quest_id` の候補だけを統合
- 初回学習時に1枚を割り当てると、同じバッチ内の画像一致候補も再判定

今回14件まで減っていたような、異なるカードの誤統合を防止します。初回は多めに候補を残し、正しいマスターを割り当てた段階で安全に重複をまとめます。

### 5. スマホ確認画面を変更

- 候補全件を並べる巨大な選択リストを廃止
- 「前へ」「次へ」で1件ずつ移動
- 現在位置を `1/20件` のように表示
- 切り出し画像の直下にマスター検索と割り当てボタンを表示
- 完全カードは「画像特徴を登録可能」と表示
- 不完全カードは保存しない理由を表示

## 初回の使い方

1. 「降臨マスター」タブで名前・属性・難易度・カテゴリを登録します。
2. スクリーンショットを抽出します。
3. 「前へ」「次へ」で切り出しカードを確認します。
4. 完全なカードに正しいマスターを選びます。
5. 「マスターを適用して画像特徴を登録」を押します。
6. 同じキャラが現在の候補内にあれば、その場で画像照合・重複統合します。

2回目以降は、登録済み画像特徴に一致したカードが自動補正されます。新キャラだけ初回割り当てが必要です。

## 更新ファイル

### GitHubで必ず上書きする実行ファイル（4件）

1. `app.py`
2. `admin_app.py`
3. `quest_master.py`
4. `video_schedule_extractor.py`

### テスト・説明ファイル（任意、3件）

5. `test_quest_master.py`
6. `test_video_schedule_extractor.py`
7. `UPDATE_v1.1.0-beta.9.13.md`

`quest_master.json` は上書きしません。既存マスターの各レコードへ `image_references` が自動追加されます。

## 確認結果

- Python構文検査：成功
- 全プロジェクト単体テスト：84件すべて成功
- 完全カードだけ画像特徴を登録できることを確認
- 欠けたカードは肖像特徴があっても登録拒否されることを確認
- OCR名なしでも肖像からマスター補正できることを確認
- 画像候補が曖昧な場合に自動確定しないことを確認
- `quest_id` 未確定候補を誤統合しないことを確認

この作業環境にはOpenCVと日本語Tesseractデータがないため、添付画像を使ったOCR本体の再実行だけは未実施です。公開環境用の依存設定は変更不要です。

## GitHubコミットメッセージ例

`feat: learn complete quest cards for visual master matching (v1.1.0-beta.9.13)`
