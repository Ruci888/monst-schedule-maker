# v1.1.0-beta.9.7 更新手順

## 今回の変更

- 管理画面の主入力を「複数スクリーンショット一括読取」に変更
- PNG・JPEGを1～10枚、合計80MBまで選択可能
- 同じカードが複数画像に重なっていても、画像署名でOCR前に重複排除
- 静止画を精密OCRし、日程・名前・属性・難易度を候補化
- 不完全な認識結果は自動公開せず、カード画像付きの修正画面へ送る
- 公開済み・承認待ちデータとの重複確認を継続
- 元画像は処理中だけメモリ上で扱い、JSONやGitHubへ保存しない
- 画面録画の読取は削除せず「実験機能」として折りたたんで残す

## 更新ファイル（全5個）

GitHubで実際に上書きする実行ファイル：

1. `app.py`
2. `admin_app.py`
3. `video_schedule_extractor.py`

動作確認・更新記録用：

4. `test_video_schedule_extractor.py`
5. `UPDATE_v1.1.0-beta.9.7.md`

`requirements.txt`と`packages.txt`の変更はありません。

## GitHubのCommit message

`Add batch screenshot OCR for beta 9.7`

## 動作確認手順

1. 上記5ファイルをGitHubへ同名で上書きする
2. Commit後、Streamlitの再起動完了を待つ
3. 管理画面の「自動取得候補・失敗ログ」を開く
4. 以前の誤認識候補が残っていれば、一括削除で消す
5. 「画像内の最初の日程」を指定する
6. スクリーンショットを5～10枚選ぶ
7. 「スクリーンショットから降臨候補を抽出」を押す
8. 確認画像を切り替え、表の名前・属性・難易度・日程を確認する
9. 必要な行だけ保存対象にして、承認待ちへ保存する

## 撮影の目安

- スマホは縦向きのまま撮る
- 文字とカード全体が表示されている状態にする
- 連続画像はカード1～2件分だけ重ねる
- 通知・キーボード・別画面を重ねない
- 画像編集や縮小をせず、そのままアップロードする

## 確認済み

- Python構文検査：成功
- 全自動テスト：55件成功

