# v1.1.0-beta.9.4 更新内容

## 更新するファイル

- `app.py`
- `admin_app.py`
- `video_schedule_extractor.py`

`test_video_schedule_extractor.py`は確認用テストです。公開環境で実行する必要はありません。

## 主な修正

- 録画の「年」ではなく「最初の日程」を指定する方式へ変更
- 指定日から14日を外れたOCR日付を候補から除外
- `3/20`など、録画期間と矛盾する日付誤認識を保存しない
- 終了日が開始日の翌日でない候補を除外
- 通常降臨の時刻を`12:00～翌11:59`へ補正
- 承認済みデータと公式候補を既知データとして名前を照合
- 一致時はキャラ名・属性・難易度を既知データで補正
- 既知データに一致しない候補は、名前・属性・難易度が揃い、複数フレームで一致した高信頼候補だけを残す
- 補正後の「日付＋キャラ名」で再度重複を除外
- 管理画面に「品質不足で除外」の件数を表示
- 公開画面のバージョン表記を`v1.1.0-beta.9.4`へ更新

## 更新後の確認手順

1. 以前の誤認識36件は承認せず、一括削除する
2. 上記3ファイルをGitHubで上書きする
3. 管理画面が再起動したら、録画で最初に表示される日付を指定する
4. 同じ動画をアップロードして抽出する
5. 「認識」「品質不足で除外」「新規候補」の件数を確認する

候補が0件になる場合も、壊れた候補を大量保存するより安全な正常動作です。既知データに未登録のキャラを増やすには、今後キャラマスターを拡充する必要があります。
